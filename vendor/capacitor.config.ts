// FIX: Removed CapacitorConfig type import and annotation to resolve a module export error.
const config = {
  appId: 'com.foodiefind.vendor',
  appName: 'FoodieFind Vendor',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
